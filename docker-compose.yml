version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 4200:4200

  # angular-app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - 8080:80

  # web:
  #   image: nginx
  #   ports: 
  #     - 80:80
  #   volumes: 
  #     - nginx.conf /etc/nginx/conf.d/default.conf  